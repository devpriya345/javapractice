spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/jdbc_tables
    username: root
    password: test

  jpa:
    show-sql: true

logging:
  level:
    org:
      springframework:
        security: DEBUG